function mergeArrays(arr1, arr2) { let newArr=arr1.concat(arr2).sort((a,b)=>{return a-b}); console.log(newArr); let a,b; let filtArr =[]; for (let i=0;i<newArr.length;i++){ a=newArr[i]; b=newArr[i+1]; if (a===b){ continue; } else{ filtArr.push(a); } } return filtArr; } //parameters: input is two arrays with integers. they are sorted but may be so in the wrong direction. //return: one array with all the integers and duplicates removed //example: [1,3,5,7,9,11,12], [1,2,3,4,5,10,12]) as the input becomes [1,2,3,4,5,7,9,10,11,12] //pseudocode
