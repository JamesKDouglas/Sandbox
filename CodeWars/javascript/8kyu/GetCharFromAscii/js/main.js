//parameters An integer representing an ascii character
//return The ascii character, as a string.
//example the code 55 should return the string "7"
//pseudocode:
//find a method in js which can do this.

function getChar(c){
  return String.fromCharCode(c);
}
